import type { CarConfig } from '@/typings/carConfig';

export const CAR: CarConfig = {
  scale: 1.6,
  position: [-0.5, -1.16, 0],
  // rotation: [0, -Math.PI / 3.1, 0],
  rotation: [0, -Math.PI, 0],
};
